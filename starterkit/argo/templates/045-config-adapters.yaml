apiVersion: v1
kind: ConfigMap
metadata:
  name: isvgimsdi
  namespace: {{ .Values.namespace }}
data:
{{ tpl (.Files.Glob "config/adapters/{*.yaml,*.sh}").AsConfig . | indent 2 }}
{{ (.Files.Glob "config/certs/{isvdi.pem,*.crt}").AsConfig | indent 2 }}
