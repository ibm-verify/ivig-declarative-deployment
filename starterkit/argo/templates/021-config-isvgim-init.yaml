apiVersion: v1
kind: ConfigMap
metadata:
  name: isvgim-init
  namespace: {{ .Values.namespace }}
data:
{{ (.Files.Glob "init/*.sh").AsConfig | indent 2 }}
binaryData:
{{ (.Files.Glob "init/*.jar").AsSecrets | indent 2 }}
