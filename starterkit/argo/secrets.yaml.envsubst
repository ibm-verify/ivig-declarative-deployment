MQLOCAL_PASSWORD: ${MQLOCAL_PASSWORD}
MQSHARE_PASSWORD: ${MQSHARE_PASSWORD}
MQADMIN_PASSWORD: ${MQADMIN_PASSWORD}
IMAGE_REGISTRY_PASSWORD: ${IMAGE_REGISTRY_PASSWORD}
LDAP_PASSWORD: ${LDAP_PASSWORD}
DB_PASSWORD: ${DB_PASSWORD}
DBADMIN_PASSWORD: ${DBADMIN_PASSWORD}
KEYSTORE_PASSWORD: ${KEYSTORE_PASSWORD}
APPSERVER_PASSWORD: ${APPSERVER_PASSWORD}
ISIMSYSTEM_PASSWORD: ${ISIMSYSTEM_PASSWORD}
LIBERTYMETRICS_PASSWORD: ${LIBERTYMETRICS_PASSWORD}
MAIL_PASSWORD: ${MAIL_PASSWORD}
OIDC_ISC_SECRET: ${OIDC_ISC_SECRET}
OIDC_REST_SECRET: ${OIDC_REST_SECRET}
OIDC_ADMINCONSOLE_SECRET: ${OIDC_ADMINCONSOLE_SECRET}

